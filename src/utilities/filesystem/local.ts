const fs = require('fs');
const path = require('path');

const upload = async (filePath: string, fileBlob: Blob): Promise<any> => {
  try {
    
  } catch (error) {
    
  }
};
exports.upload = upload;

const remove = async (filePath: string): Promise<any> => {
  try {
    
  } catch (error) {
    
  }
};
exports.remove = remove;

exports.update = async (
  oldFilePath: string,
  newFilePath: string,
  newFileBlob: Blob,
): Promise<any> => {
  try {
    
  } catch (error) {
    
  }
};
